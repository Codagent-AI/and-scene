# Presentation template

Copy this directory into `src/presentations/<slug>/`. Rename the entities
namespace, replace the example step, add additional step modules, and register
the presentation explicitly in `src/presentations/index.ts`.
