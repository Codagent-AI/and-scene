# Presentation template

Copy this directory into `src/presentations/<slug>/`. Rename the entities
namespace, replace `steps/first.tsx`, add additional step modules under
`steps/`, and register the presentation explicitly in
`src/presentations/index.ts`.
