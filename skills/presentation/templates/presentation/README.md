# Presentation template

Copy this directory to `src/presentations/<slug>/` (or the equivalent app
source directory), replace the `{{...}}` placeholders, expand `STEPS`, and add
one lazy entry to the presentation registry.
