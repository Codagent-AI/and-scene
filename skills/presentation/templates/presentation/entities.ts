/** Stable, presentation-local layout identities. Prefix every ID with this presentation's slug. */
export const entities = {
  subject: 'your-presentation:subject',
  prompt: 'your-presentation:prompt',
  connection: 'your-presentation:connection',
} as const
