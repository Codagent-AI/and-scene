import type { Step } from '../../../presentation-kit'
import { exampleStep } from './01-example'
import type { Payload } from './payload'

// Replace with the full ordered list of this presentation's steps, in order.
export const STEPS: Step<Payload>[] = [exampleStep]
